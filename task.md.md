\*"Tôi đã lưu ảnh preview vào thư mục `public` và đặt tên là `og-image.jpg`. Hãy cập nhật file `index.html` của tôi:

1. Sử dụng link tuyệt đối sau cho thẻ `og:image`: `https://quantum-rebirth-os.vercel.app/og-image.jpg`  
2. Đảm bảo thẻ `twitter:image` cũng dùng link này.  
3. Cập nhật `og:url` thành `https://quantum-rebirth-os.vercel.app/`

Hãy viết lại đoạn mã các thẻ Meta chuẩn xác nhất."\*

